
#if 0
#include <type/primitive/kind.h>

struct types;
struct literal_expression;

int (*cast_constfolders[number_of_primitive_types])
	(struct literal_expression**, struct literal_expression*, struct types*);
#endif
