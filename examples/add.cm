
extern void printf(char*, ...), exit(int);

void main ()
{
	int i, j, k, l, m;
	
	printf("%i\n", 10 + 20); // 30
	
	i = 1; j = 2L, k = 3U; l = 4UL; // added suffixes for fun
	m = i + j + k + l;
	printf("%i\n", m); // 10
	
	exit(0);
}

