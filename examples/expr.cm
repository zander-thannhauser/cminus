
void exit();

int a;
char b;
float c;
int d[10];

void main()
{

   if (b == b)
    {
     a = a + a;
     c = c * a;
     c = a - c;
    }

   if (c >= a) {
     b = b || b;
    }

   a = a && c;

   exit();
}
